# PostgreSQL Database Configuration
# The canonical .env is in listings-visualization/.env
# Scrapers read these same vars when running on Apify (set as Actor env vars)
DB_HOST=postgresql-v1mr4-u45404.vm.elestio.app
DB_PORT=25432
DB_NAME=bazos_scraper
DB_USER=postgres
DB_PASSWORD=<your_password>
DB_SSL_MODE=require

# Apify API token (for scheduler to trigger scraper runs)
APIFY_TOKEN=<your_apify_token>

# Server Configuration
PORT=3000

# Session secret (change in production)
SESSION_SECRET=<random_secret>
